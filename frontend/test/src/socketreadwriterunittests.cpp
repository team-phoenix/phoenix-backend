#include "catch.hpp"
#include "socketreadwriter.h"

SCENARIO("SocketReadWriter")
{
  GIVEN("a socket has more than 4 bytes of data to be read") {
//    SocketReadWriter subject;

//    struct MockSocket {

//      int bytesRead = 0;
//      int bytesToRead = 10;
//      QByteArray message = "{}";
//      bool bytesAreAvailable = true;

//      bool bytesAvailable() {return bytesAreAvailable;}
//      int read(char* dest, size_t byteSize)
//      {
//        *dest = *(reinterpret_cast<char*>(&bytesToRead));
//        bytesRead = byteSize;
//        return bytesRead;
//      }
//    };

//    MockSocket mockSocket;

//    WHEN("readSocketMessage is called") {
//      const QVariantMap message = subject.readSocketMessage(mockSocket);
//      THEN("the returned message contains all of the original json message") {
//        REQUIRE();
//      }
//    }
  }
}
